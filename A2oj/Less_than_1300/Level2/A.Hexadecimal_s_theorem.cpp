#include <bits/stdc++.h>

int main(int argc, char const *argv[])
{
    int n; 
    std::cin >> n;
    std::cout << 0 << " " << 0 << " " << n; // zero is a fibonacci number and the number it self is also a fibonacci number
    return 0;
}
